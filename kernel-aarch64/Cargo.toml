[package]
name = "kernel-aarch64"
version = "0.1.0"
edition = "2021"

[dependencies]
kernel-core = { path = "../kernel-core" }
boot-uefi = { path = "../boot-uefi" }

[[bin]]
name = "kernel"
path = "src/main.rs"
